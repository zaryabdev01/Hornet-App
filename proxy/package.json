{
  "name": "apisave-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "ApiSave M3 — server-side proxy for Gemini vision calls. Holds the real Gemini API key server-side so it never ships inside the mobile app bundle.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
